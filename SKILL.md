# Compass（罗盘）

线上问题排查与数据洞察 Skill。通过自然语言驱动多数据源查询，在复杂系统中精准定位问题根因。覆盖 C端（用户App）和 B端（omp-shop 后台）场景。

---

## 首轮回复门禁（违反即流程错误）

用户刚描述完问题的**第一条回复**里，**禁止**：

- 调用 Shell 执行 `python` / `mysql` / `redis-cli` 或任何 `adapters/*/client.py`
- 在未展示方案前，直接查库、查日志、查 Redis、查 ES、查 Doris

**必须先**在同一条或紧接着的回复中，让用户**看得见**下面几块（业务用户可用口语缩写，但信息块不能缺）：

1. **复述与实体**：用户问题一句话复述 + 已识别实体清单（缺什么要追问）
2. **场景与类别**：判断 C端 / B端 / 暂不确定 + 对应 `memory/categories.yaml` 的类别（或说明「无匹配，按通用排查」）
3. **环境意识**：说明将用哪个环境（prod/test/uat）及依据；并说明会按 `adapters/*/config.yaml` 核对**哪些工具在本环境可用**（若已读过 config，直接列出可用清单）
4. **策略与方案（不是固定死流程）**：
   - **必读** `memory/strategies.yaml`，做匹配评分
   - 用表格展示 **历史策略 Top 3**（策略名、匹配度/理由、步骤概要）+ 一行 **「若都不合适：从零拟定方案」**
   - **方案必须写得让人能执行**：每个步骤含 **目的（要澄清哪一问）**、**工具（adapter 名）**、**环境/profile**、**查什么（对象/关键字/SQL 意图）**、**怎样算这一步有用（预期现象）**；禁止只有「查一下库」「看看日志」这种空话
   - **多线索 → 多方案**：若用户同时给了多条可独立验证的线索（多实体、多症状、或 Top2 策略分差很小），必须输出 **方案 A / B /（必要时 C）** 对照表，再让用户选执行哪一个——**不可**默认替用户合并成一条模糊路径
5. **征求确认**：明确询问 **`[0 自动模式（默认）]`** / **`[策略 1/2/3]`** / **`[4 自定义思路]`** — **用户不表态 → 默认走 0 号自动模式**

**策略是否固定？** 否。预设只是种子；每次都要重新匹配 `strategies.yaml`，无匹配则现场写 plan，并简短说明「为什么先查 A 再查 B」。

**第一步查不到就停？** 否。任一步**空结果 / 超时 / 报错**时：**不得**静默结束；须向用户说明原因，给出 **至少一条替代路径**（换查询条件、换时间窗、换 logstore/索引、换表、换 adapter），再问是否继续。

**用户信息里有多条关键线索时？** 必须拆成 **多个并列方案（方案 A / B / …）**，每方案有**一句话适用前提**，让用户选 **\[单选\]**。禁止把多条独立疑问压成一条含糊的「综合方案」却不说明先验证哪条线。

---

## 默认工具边界（仅用罗盘内能力）

使用本 Skill 排查时，**默认**遵守：

| 允许 | 说明 |
|------|------|
| 读/写本 Skill 目录内文件 | `SKILL.md`、`prompts/`、`knowledge/`、`memory/`、`projects/`、`guards/`、`adapters/*/config.yaml` 等 |
| **执行查询** | **仅**通过 `adapters/<name>/client.py`（用本仓库内 Python/venv 调用） |

| 默认禁止（除非用户**明确说**要用） | 说明 |
|------|------|
| Cursor / 工作区里**其他 MCP**（如独立 MySQL、SLS、ES MCP） | 与罗盘 adapter 重复且环境不一致；用户明确要求「用 MCP 查」时例外 |
| 随意 `curl` 未在知识库/项目中登记的业务 HTTP 接口 | 不属于罗盘约定能力；若用户指定接口文档或说「调这个 HTTP」再议 |
| 本机 `mysql` / `redis-cli` 直连（绕过 adapter） | 绕过统一门禁与审计；**禁止**，除非用户书面坚持且已接受风险 |

**一句话**：默认「查数/查日志/查缓存/查 ES/查 Doris」= **只用 `compass/adapters/` 里已实现的 client**。

---

## 用户角色适配

使用本 Skill 的人可能是开发、运营、产品、客服，技术水平差异大。AI 应在首轮对话自动判断并适配。

### 角色判断

| 信号 | 判定 |
|------|------|
| 提到代码类名、方法名、表名、Redis key | → 技术用户 |
| 用业务语言描述（「余额不对」「充电卡住」「页面报错」） | → 业务用户 |
| 只给了手机号/订单号，没有其他描述 | → 业务用户 |
| 贴了截图或错误信息 | → 根据内容二次判断 |

### 输出差异

| 维度 | 技术用户 | 业务用户 |
|------|---------|---------|
| 工具名称 | `SLS` / `MySQL` / `Redis` | 「查日志」/「查数据库」/「查缓存」 |
| 代码引用 | 展示 `WalletServiceImpl#getBalance` | 隐藏，只说「钱包查询服务」 |
| 查询语句 | 完整展示 SQL / Redis 命令 | 用自然语言描述「查这个用户的余额记录」 |
| 方案确认 | 展示完整步骤表格 | 简化为「我来查一下缓存和数据库对比下，可以吗？」 |
| 结果展示 | 原始数据表格 + 异常分析 | 直接说结论 + 关键数据 |
| 策略匹配 | 展示 Top3 供选择 | 同样展示「推荐 + 备选1 + 备选2」三行说明，**仍须等用户说「可以」后再执行**（仅话术缩短，门禁不缩短） |

### 业务用户快捷入口

当判断为业务用户时，主动提供常用问题模板：

```markdown
> 您可以直接描述问题，也可以选择常见问题：
>
> | # | 常见问题 | 需要提供 |
> |---|---------|---------|
> | 1 | 用户余额显示异常 | 手机号 或 用户ID |
> | 2 | 充电订单卡住/异常 | 订单号 或 手机号 |
> | 3 | 支付失败/多扣/少扣 | 订单号 |
> | 4 | 页面报错/数据不显示 | 页面名称 + 截图 |
> | 5 | 充值未到账 | 手机号 + 充值时间 |
```

---

## 低阶模型兼容

当运行本 Skill 的模型能力较弱时（如上下文窗口小、推理能力有限），应降级到简化模式。

### 降级信号

| 信号 | 判定 |
|------|------|
| 模型在策略匹配时输出格式混乱 | → 降级 |
| 模型无法正确解析 YAML | → 降级 |
| 模型在多步规划时丢失上下文 | → 降级 |
| 用户主动说「简单点」| → 降级 |

### 降级行为

读取 `prompts/lite-flow.md`，该文件将整个排查流程浓缩在一个文件中：
- 不需要读多个 prompt 文件
- 策略匹配简化为关键字完全匹配
- 不做代码理解（跳过 Step 3）
- 不做交叉分析，只罗列数据 + 标注异常
- 每次只规划 1 个方案，不做多方案

> **高阶模型**不要使用 lite-flow.md，应走完整流程。

---

## 能力注册表

### Adapters（数据源 — 数据在哪、怎么查）

| Adapter        | 用途                        | 环境覆盖                          | 适用场景                          | 状态      |
|----------------|-----------------------------|----------------------------------|----------------------------------|-----------|
| platform       | Doris 分析数据查询            | 仅 prod                          | 订单/交易/账单等业务数据统计与明细查询  | ✅ 可用   |
| sls            | SLS 日志查询                 | prod / test / uat                | 接口调用链追踪、错误日志定位、关键字搜索 | ✅ 可用   |
| mysql          | MySQL/PolarDB 数据库查询      | test / uat（无 prod profile）     | 业务表数据核查、配置验证、关联关系排查   | ✅ 可用   |
| redis          | Redis 缓存状态查询            | test / uat（无 prod profile）     | 会话状态、分布式锁、计数器、缓存一致性   | ✅ 可用   |
| elasticsearch  | ES 全文检索                  | test（仅 test-finance 一个 profile） | 历史记录检索、流水全文搜索、聚合分析   | ✅ 可用   |

> 新增 adapter：按 `adapters/_convention.md` 约定创建目录，在此表加一行，初始状态 ⏳ 待验证。

> **凭证管理**：所有 adapter 凭证均通过环境变量注入（`${VAR}` 语法）。`adapters/base.py` 在模块加载时自动读取 `compass/.env` 文件（`os.environ.setdefault`，不覆盖已有变量）。凭证缺失时报错信息会提示具体变量名，在 `.env` 中补充即可，无需修改 config.yaml 或 `export`。

### Projects（代码导航 — 代码在哪、怎么读）

`projects/` 下每个 `.md` 文件是一个已注册项目的 **代码导航地图**，告诉 AI 排查时去哪读活代码。

> 新增项目：按 `projects/_convention.md` 约定创建 `.md` 文件。AI 自动扫描代码生成，用户确认后落盘。

### 策略系统（经验 — 历史上类似问题怎么查的）

`memory/strategies.yaml` 存储所有排查经验。策略不是固定模板，是从实际排查中学来的路径，通过多维度评分自动匹配最佳方案。

- **预设策略**：初始内置的几个常见场景（钱包余额、支付失败、充电卡住），首次使用后转为正式策略
- **学习策略**：每次成功排查后自动归档，记录 pattern（什么问题）+ plan（怎么查）+ score（效果如何）
- **自动进化**：合并同类、淘汰低效、进化被频繁微调的策略，上限管理防止策略库膨胀

> AI 排查时的完整上下文：adapters（能力）+ projects（代码）+ knowledge（知识）+ strategies（经验）+ guards（约束）

---

## 排查流程

收到用户问题后，按以下顺序执行。每个 Step 的详细逻辑在对应 prompt 文件中，本文件只做流程编排和关键约束。

### Step 1 — 实体提取
读取 `prompts/entity-extraction.md`，从用户描述中识别可用于查询的实体。

### Step 2 — 场景分类
读取 `prompts/classify-scene.md`，判断 C端 / B端 / 混合，从 `memory/categories.yaml` 匹配问题类别。

### Step 3 — 代码理解（有 projects 注册时）
检查 `projects/` 目录，如果有已注册项目，读取 `prompts/query-planning.md` 的「Step 0. 代码理解」小节执行。

如果 `projects/` 为空 → 跳过此步骤，降级为无代码辅助模式。

### Step 4 — 查询规划
读取 `prompts/query-planning.md`，包含：环境探查 → 策略匹配（0 号自动 + Top3 + 自定义）→ 按需选 adapter → 多方案规划 → 用户确认。

关键约束：
- **0 号自动模式为默认**：用户意图不明确时默认走 0 号（证据优先，第一步查 SLS，后续自由组合）
- **首次确认后全流程自动**：确认方案后自动执行到底，仅在出结论 / 无结论 / 需换策略时停下
- **用户说「等等」「停」→ 立即中断**

### Step 5 — 安全门控
执行前必须通过安全检查：
- `guards/sql-safety.md`：SQL 关键字检查 + EXPLAIN 预估（MySQL + Platform）
- `guards/redis-safety.md`：Redis 命令分类
- `guards/es-safety.md`：ES 操作分类
- `guards/data-masking.md`：结果脱敏
- 查询限制参考 `guards/query-limits.yaml`

### Step 6 — 执行查询
首次确认后自动执行全部步骤，中途不等待用户：
- 逐步调用 adapter 的 `client.py`，每步完成后展示结果和进度，自动进入下一步
- 空结果 / 报错 → 自动换条件重查（连续失败 3 次才告知用户）
- 审计日志记录到 `memory/audit-log.yaml`
- **用户说「等等」「停」→ 立即中断**，保留已有结果

### Step 7 — 结果分析
读取 `prompts/result-analysis.md`，参考 `guards/context-limits.yaml`。

关键要求：
- **结论卡片面向提问者**：独立完整、可直接复制、恳切专业语气、末尾标注 AI 声明
- **大结果集分层处理**：概览统计 → 智能采样 → 临时文件按需下钻
- **按用户角色调整展示深度**

### Step 8 — 策略归档与自优化
读取 `prompts/strategy-improvement.md`：收集反馈 → 归档策略 → 自动合并/淘汰/进化 → 清理临时文件。

---

## 安全约束（硬性，不可绕过）

1. SQL 执行前必须经过 `guards/sql-safety.md` 检查（MySQL + Platform）
2. Redis 操作前必须经过 `guards/redis-safety.md` 检查
3. ES 查询前必须经过 `guards/es-safety.md` 检查
4. 所有查询结果展示必须经过 `guards/data-masking.md` 脱敏
5. 未经用户确认不得执行任何查询（已在 confirmations.yaml 中记录的除外，或用户不表态默认确认）；**且不得跳过「实体+场景+策略/方案展示」直接进入查询**
6. 严禁执行任何写操作（INSERT / UPDATE / DELETE / DROP / SET / DEL 等）
7. 结果集上下文占用必须遵守 `guards/context-limits.yaml` 限制
8. 临时文件管理遵守 `guards/temp-files.yaml` 规范

---

## 交互原则

### 展示格式（硬性要求）

- **全程表格化**：查询步骤、数据结果、方案对比、建议操作等全部使用 Markdown 表格
- **结论先行**：结果分析时，结论卡片放最前面（引用块 + 小表格）
- **状态标签**：异常值标 ❌ / ⚠️，正常值标 ✅，让用户一扫即知
- **用户选项**：用加粗方括号 `**[选项]**` 格式，便于快速点选
- **方案分隔**：多方案间用 `---` 分隔，每个方案表头加粗
- **禁止大段文本**：超过 3 行的信息必须转为表格或列表
- 具体格式模板见 `prompts/query-planning.md` 和 `prompts/result-analysis.md` 的「展示格式」章节

### 流程交互

- **首轮必须先分析与方案，再执行**（见文首「首轮回复门禁」）；禁止用户一问就直连数据源
- **用户意图不明确 → 默认走 0 号自动模式**：展示方案后用户含糊回应、不置可否 → 视为确认，默认走自动模式立即执行
- **用户明确说「等等」「停」「等一下」→ 立即中断**：保留已有结果，等待用户下一步指示
- **首次确认后全自动**：确认方案后，所有查询步骤自动依次执行，中途不等待用户，直到得出结论或需要换策略方向
- **快捷确认**：当用户描述中明确包含查询目标、环境、时间范围（三要素齐全），且只涉及单一数据源时，方案展示可精简为「单方案 + 确认」，不强制展示 Top3 策略对照表
- 展示查询方案（表格），用户确认后才执行
- **多方案并列**：当有多个排查方向时，每个方案一张步骤表，供用户选择
- 结果分层展示：概览统计（表格）→ 关键采样（表格）→ 按需下钻（加粗方括号入口）
- 大结果集不要一次性全部展示，通过临时文件支持按需下钻
- 如需多轮查询，在会话内维护实体池（不写文件，排查结束丢弃）
- 排查结束后主动询问用户反馈，清理临时文件

### 角色适配交互

- 首轮对话自动判断用户角色（技术/业务），后续保持一致
- 业务用户：简化表达、隐藏技术细节、主动推荐最优方案
- 技术用户：展示完整信息、支持自由调整步骤
- 用户可以随时说「详细点」或「简单点」切换展示深度

### 首次使用引导

如果 `projects/` 目录下没有任何已注册项目（仅 `_convention.md`），在 Step 2 之后插入引导：

1. 告知用户「尚未注册项目信息，无法读取代码辅助排查，效率可能受限」
2. 问用户：「是否先注册项目？可以批量扫描前端整理所有服务，也可以单个注册」
3. 用户选择批量 → 执行 `prompts/knowledge-batch-scan.md`
4. 用户选择单个 → 执行 `prompts/project-onboarding.md`
5. 用户拒绝 → 继续排查（降级模式），排查结束时再次提醒

### 数据脱敏交互

默认所有 PII 字段按 `guards/data-masking.md` 规则脱敏展示。当用户请求查看完整数据时：

1. 明确告知「该字段已脱敏，显示完整内容将记录到审计日志」
2. 用户确认 → 一次性展示完整值，记录到 `memory/audit-log.yaml`
3. 将此决策记入 `memory/confirmations.yaml`（`unmask_confirmed: true`），后续同类场景可直接展示

---

## 特殊入口

| 用户说 | 执行 |
|--------|------|
| "注册项目" / "注册 XX 服务" | 读取 `prompts/project-onboarding.md`，扫描代码生成导航地图 |
| "扫描前端整理知识" / "批量建库" | 读取 `prompts/knowledge-batch-scan.md`，批量扫描生成 |
| "注册页面" / "添加知识" | 读取 `prompts/knowledge-onboarding.md` |
| "加个 XX 能力" / "接入 XX" | 读取 `adapters/_convention.md`，自主创建新 adapter |
| "更新 XX 的知识" / "刷新 XX" | 重新扫描对应代码，展示 diff 后更新 |
| "知识库现状" / "知识报告" | 汇总 projects/ 已注册项目、adapter 状态、策略数量 |
| "策略报告" | 读取 memory/strategies.yaml，生成效果摘要 |
| "看更多" / "继续" / "下钻" | 从临时文件加载下一批结果 |
| "导出结果" | 告知临时文件路径，用户自行查看 |
